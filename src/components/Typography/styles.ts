import { SxClasses } from 'src/utils/styles';

const sx: SxClasses = {
    ellipsis: {
        overflow: 'hidden',
        textWrap: 'nowrap',
        textOverflow: 'ellipsis'
    }
};

export default sx;
