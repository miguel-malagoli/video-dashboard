import { SxClasses } from 'src/utils/styles';

const sx: SxClasses = {
    flexBox: {
        display: 'flex'
    },
    flexColumn: {
        display: 'flex',
        flexDirection: 'column'
    },
    flexCenter: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center'
    }
};

export default sx;
